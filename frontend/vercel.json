{
  "version": 2,
  "name": "betcompare-frontend",
  "regions": ["iad1"],
  "routes": [
    // ... (keep your existing routes array)
  ],
  "env": {
    // ... (keep your existing env variables)
  },
  "headers": [
    // ... (keep your existing headers)
  ],
  "redirects": [
    // ... (keep your existing redirects)
  ],
  "rewrites": [
    // ... (keep your existing rewrites)
  ],
  "functions": {
    "pages/api/revalidate.js": {
      "maxDuration": 30
    }
  },
  "crons": [
    {
      "path": "/api/revalidate",
      "schedule": "0 */6 * * *"
    }
  ],
  "github": {
    "enabled": true,
    "autoAlias": true
  },
  // Remove these if not needed (Vercel usually detects them automatically):
  // "buildCommand": "npm run build",
  // "outputDirectory": ".next",
  // "installCommand": "npm install",
  // "devCommand": "npm run dev"
}